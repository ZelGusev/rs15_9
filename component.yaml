name: rs

sources:
  - path: $local/
    tag: ''
    files:
      build:
        - rtl/rtl.files
xrun:
  top: rs
  run: -timescale 1ns/1ns

targets:
  dc-check:
    - cd ../../dc/common/scripts
    - ./run.sh